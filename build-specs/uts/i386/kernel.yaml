toolchain:
  cc: gcc
  cpp: cpp
  ld: ld
  cflags:
    - -std=gnu89
    - -fcommon
    - -fno-builtin
    - -fno-unwind-tables
    - -fno-asynchronous-unwind-tables
    - -O2
    - -m32
    - -Werror
    - -DREL="4"
    - -DVER="2.1"
    - -DSYSV
  cppflags:
    - -P
  ldflags: []

variables:
  system_root: "{builddir}/uts/i386/system-root"
  conf_root: "{builddir}/uts/i386/conf"
  cf_dir: "{conf_root}/cf.d"
  pack_root: "{conf_root}/pack.d"
  obj_root: "{builddir}/uts/i386/obj"
  legacy_stage_root: "{builddir}/legacy-src"
  boot_stage_root: "{legacy_stage_root}/uts/i386/boot"
  boot_stage_at386_root: "{boot_stage_root}/at386"
  reference_boot_floppy: "{workspace_root}/original_diskettes/Base 01 (2.1a).img"
  hybrid_boot_floppy: "{builddir}/boot-media/base01-hybrid.img"
  hybrid_boot_report: "{builddir}/boot-media/base01-hybrid-layout.json"
  hybrid_boot_replacement_manifest: "{workspace_root}/build-specs/uts/i386/boot-floppy-replacements.json"
  hybrid_boot_replacement_set: "label-hybrid"
  hybrid_boot_labeled_floppy: "{builddir}/boot-media/base01-hybrid-labeled.img"
  hybrid_boot_labeled_report: "{builddir}/boot-media/base01-hybrid-labeled-layout.json"
  boot_floppy: "{builddir}/boot-media/base01-boot.img"
  boot_report: "{builddir}/boot-media/base01-boot-layout.json"
  boot_replacement_set: "kernel-at386"
  boot_labeled_floppy: "{builddir}/boot-media/base01-boot-labeled.img"
  boot_labeled_report: "{builddir}/boot-media/base01-boot-labeled-layout.json"
  boot_labeled_replacement_set: "kernel-at386-labeled"
  floppy_cf_dir: "{conf_root}/cf.d.floppy"
  floppy_idconfig_manifest: "{floppy_cf_dir}/simple-idconfig.json"
  floppy_driver_compose_report: "{floppy_cf_dir}/driver-compose-report.json"
  floppy_unix: "{floppy_cf_dir}/unix"
  boot_kernel_stripped: "{builddir}/boot-media/unix.at386.floppy"
  fp_stage_root: "{legacy_stage_root}/uts/i386/fp"
  idcmd_tool_root: "{builddir}/uts/i386/tools/idcmd"
  conf_bin: "{system_root}/etc/conf/bin"
  simple_idconfig_manifest: "{cf_dir}/simple-idconfig.json"
  driver_compose_report: "{cf_dir}/driver-compose-report.json"
  init_root: "{conf_root}/init.d"
  mfsys_root: "{conf_root}/mfsys.d"
  node_root: "{conf_root}/node.d"
  rc_root: "{conf_root}/rc.d"
  sd_root: "{conf_root}/sd.d"
  mdevice_root: "{conf_root}/mdevice.d"
  sdevice_root: "{conf_root}/sdevice.d"
  sfsys_root: "{conf_root}/sfsys.d"

source_views:
  uts-at386:
    roots:
      - arch/at/uts/i386
      - uts/i386

profiles:
  kernmap-at386:
    flags:
      - -I.
      - -D_KERNEL
      - -DAT386
      - -DWEITEK
      - -DWEITEK_EMULATOR

  os-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DAT386
      - -DWEITEK

  vm-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -Dnotnow
      - -DAT386

  exec-at386:
    flags:
      - -I../..
      - -D_KERNEL
      - -DAT386

  io-at386:
    include_source_view_roots: true
    flags:
      - -I..
      - -I../..
      - -D_KERNEL
      - -DSYSV
      - -DSVR40
      - -DWEITEK
      - -DAT386

  fs-at386:
    flags:
      - -I..
      - -I../..
      - -D_KERNEL
      - -DQUOTA
      - -DAT386

  disp-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DAT386

  vx-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DAT386

  netmod-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DSYSV
      - -DAT386

  netinet-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DSYSV
      - -Di386
      - -DAT386

  kdb-at386:
    flags:
      - -I../..
      - -D_KERNEL
      - -DAT386

  ml-at386:
    flags:
      - -I..
      - -D_KERNEL
      - -DAT386
      - -DWEITEK
      - -DWEITEK_EMULATOR

targets:
  prepare-layout:
    description: Create the full AT386 kernel object and conf tree roots used by the YAML-driven build graph.
    steps:
      - kind: mkdirs
        paths:
          - "{conf_root}"
          - "{cf_dir}"
          - "{pack_root}"
          - "{obj_root}"
          - "{init_root}"
          - "{mfsys_root}"
          - "{node_root}"
          - "{rc_root}"
          - "{sd_root}"
          - "{mdevice_root}"
          - "{sdevice_root}"
          - "{sfsys_root}"

  config-at386:
    description: Materialize the top-level preprocessed kernel configuration inputs that feed the final system build.
    depends_on:
      - prepare-layout
    steps:
      - kind: preprocess
        source_view: uts-at386
        use:
          - kernmap-at386
        cwd: .
        source: kernmap
        output: "{cf_dir}/kernmap"

      - kind: preprocess
        source_view: uts-at386
        use:
          - kernmap-at386
        cwd: .
        source: vuifile
        output: "{cf_dir}/vuifile"

  master-tree-at386:
    description: Stage the historical uts/i386/master.d and arch/at/uts/i386/master.d metadata into the synthetic conf tree.
    depends_on:
      - config-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_stage_master_at386.py
          - --workspace-root
          - "{workspace_root}"
          - --conf-root
          - "{conf_root}"
          - --cpp
          - "{cpp}"
          - --cpp-flag=-P
          - --cpp-flag=-DAT386
          - --cpp-flag=-DWEITEK
          - --cpp-flag=-DWEITEK_EMULATOR

  simple-idconfig-at386:
    description: Generate a modern config.h and configuration manifest from the staged AT386 conf metadata without using the legacy idtools.
    depends_on:
      - master-tree-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_idconfig.py
          - --conf-root
          - "{conf_root}"
          - --output-dir
          - "{cf_dir}"
          - --manifest
          - "{simple_idconfig_manifest}"
          - --obj-root
          - "{obj_root}"

  simple-idconfig-at386-floppy:
    description: Generate a reduced AT386 config and manifest for a floppy-sized kernel by excluding non-essential modules from the staged historical metadata.
    depends_on:
      - master-tree-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_idconfig.py
          - --conf-root
          - "{conf_root}"
          - --output-dir
          - "{floppy_cf_dir}"
          - --manifest
          - "{floppy_idconfig_manifest}"
          - --obj-root
          - "{obj_root}"
          - --exclude-module
          - bfs
          - --exclude-module
          - cmux
          - --exclude-module
          - fdfs
          - --exclude-module
          - hd
          - --exclude-module
          - i286x
          - --exclude-module
          - intp
          - --exclude-module
          - kdb
          - --exclude-module
          - arp
          - --exclude-module
          - ip
          - --exclude-module
          - krpc
          - --exclude-module
          - ktli
          - --exclude-module
          - prf
          - --exclude-module
          - raio
          - --exclude-module
          - rawip
          - --exclude-module
          - RFS
          - --exclude-module
          - rt
          - --exclude-module
          - sockmod
          - --exclude-module
          - tcp
          - --exclude-module
          - ticlts
          - --exclude-module
          - ticots
          - --exclude-module
          - ptm
          - --exclude-module
          - pts
          - --exclude-module
          - sem
          - --exclude-module
          - shm
          - --exclude-module
          - ticotsor
          - --exclude-module
          - sxt
          - --exclude-module
          - timod
          - --exclude-module
          - ufs
          - --exclude-module
          - udp
          - --exclude-module
          - vx
          - --exclude-module
          - xnamfs
          - --exclude-module
          - xout
          - --exclude-module
          - xt

  driver-packages-at386:
    description: Compose pack.d/<module>/Driver.o files from the YAML-driven object tree and the generated uts_idconfig manifest.
    depends_on:
      - kernel-at386
      - simple-idconfig-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_driver_compose.py
          - --manifest
          - "{simple_idconfig_manifest}"
          - --workspace-root
          - "{workspace_root}"
          - --cc
          - "{cc}"
          - --ld
          - "{ld}"
          - --cflag=-std=gnu89
          - --cflag=-fcommon
          - --cflag=-fno-builtin
          - --cflag=-O2
          - --cflag=-m32
          - --cflag=-D_KERNEL
          - --cflag=-DAT386
          - --cflag=-DWEITEK
          - --cflag=-DWEITEK_EMULATOR
          - --cflag=-DVPIX
          - --cflag=-DSYSV
          - --cflag=-DSVR40
          - --include-dir={cf_dir}
          - --include-dir={workspace_root}/arch/at/uts/i386
          - --include-dir={workspace_root}/uts/i386
          - --report
          - "{driver_compose_report}"

  driver-packages-at386-floppy:
    description: Compose the reduced pack.d/<module>/Driver.o set for the floppy-sized AT386 kernel.
    depends_on:
      - kernel-core-at386
      - simple-idconfig-at386-floppy
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_driver_compose.py
          - --manifest
          - "{floppy_idconfig_manifest}"
          - --workspace-root
          - "{workspace_root}"
          - --cc
          - "{cc}"
          - --ld
          - "{ld}"
          - --cflag=-std=gnu89
          - --cflag=-fcommon
          - --cflag=-fno-builtin
          - --cflag=-O2
          - --cflag=-m32
          - --cflag=-D_KERNEL
          - --cflag=-DAT386
          - --cflag=-DWEITEK
          - --cflag=-DWEITEK_EMULATOR
          - --cflag=-DVPIX
          - --cflag=-DSYSV
          - --cflag=-DSVR40
          - --include-dir={floppy_cf_dir}
          - --include-dir={workspace_root}/arch/at/uts/i386
          - --include-dir={workspace_root}/uts/i386
          - --report
          - "{floppy_driver_compose_report}"

  idcmd-tools-at386:
    description: Build host-side idconfig and idmkunix binaries for the staged historical idbuild flow.
    steps:
      - kind: command
        argv:
          - sh
          - -eu
          - -c
          - |
              mkdir -p "{idcmd_tool_root}"
              "{cc}" -std=gnu89 -fcommon -fno-builtin -O2 -DHOST_IDCMD_BUILD -D_STYPES -DAT386 -DVPIX -DWEITEK -DMERGE \
                -I"{workspace_root}/cmd/idcmd" \
                "{workspace_root}/cmd/idcmd/idconfig.c" -o "{idcmd_tool_root}/idconfig"
              "{cc}" -std=gnu89 -fcommon -fno-builtin -O2 -D_STYPES -DAT386 -DVPIX -DWEITEK -DMERGE \
                -I"{workspace_root}/cmd/idcmd" \
                "{workspace_root}/cmd/idcmd/idmkunix.c" -o "{idcmd_tool_root}/idmkunix"

  stage-idbuild-root-at386:
    description: Stage the synthetic conf tree into a ROOT-shaped directory suitable for the historical idbuild script.
    depends_on:
      - driver-packages-at386
      - master-tree-at386
      - idcmd-tools-at386
    steps:
      - kind: command
        argv:
          - sh
          - -eu
          - -c
          - |
              rm -rf "{system_root}/etc/conf"
              mkdir -p "{system_root}/etc"
              cp -a "{conf_root}" "{system_root}/etc/conf"
              mkdir -p "{conf_bin}" "{system_root}/usr/bin" "{system_root}/usr/lib" "{system_root}/bin" "{system_root}/lib"
              cp "cmd/idcmd/idbuild" "{conf_bin}/idbuild"
              chmod 755 "{conf_bin}/idbuild"
              for tool in idconfig idmkunix; do
                cp "{idcmd_tool_root}/$tool" "{conf_bin}/$tool"
                chmod 755 "{conf_bin}/$tool"
              done
              cat >"{system_root}/usr/bin/idas" <<'EOF'
              #!/bin/sh
              set -eu
              exec as --32 "$@"
              EOF
              cat >"{system_root}/usr/bin/idld" <<'EOF'
              #!/bin/sh
              set -eu
              if [ "$#" -gt 0 ] && [ "$1" = "-V" ]; then
                printf "%s\n" "'(SCDE) 5.0'"
                exit 0
              fi
              exec python3 "{workspace_root}/tools/legacy_ld.py" "$@"
              EOF
              cat >"{system_root}/usr/bin/idcc" <<'EOF'
              #!/usr/bin/env python3
              import os
              import sys

              filtered = [
                  arg for arg in sys.argv[1:]
                  if not (arg.startswith('-Y0,') or arg.startswith('-Y2,') or arg.startswith('-Ya,'))
              ]
              os.execvp("{cc}", ["{cc}", "-m32", "-std=gnu89", "-fcommon", "-fno-builtin", *filtered])
              EOF
              cat >"{system_root}/usr/lib/idcpp" <<'EOF'
              #!/bin/sh
              set -eu
              exec "{cpp}" "$@"
              EOF
              cat >"{system_root}/usr/lib/idacomp" <<'EOF'
              #!/bin/sh
              echo "idacomp wrapper should not be used in ELF mode" >&2
              exit 1
              EOF
              chmod 755 "{system_root}/usr/bin/idas" "{system_root}/usr/bin/idld" "{system_root}/usr/bin/idcc" "{system_root}/usr/lib/idcpp" "{system_root}/usr/lib/idacomp"
              ln -sf ../usr/bin/idas "{system_root}/bin/idas"
              ln -sf ../usr/bin/idld "{system_root}/bin/idld"
              ln -sf ../usr/bin/idcc "{system_root}/bin/idcc"
              ln -sf ../usr/lib/idcpp "{system_root}/lib/idcpp"
              ln -sf ../usr/lib/idacomp "{system_root}/lib/idacomp"

  idbuild-at386:
    description: Build the final AT386 unix image with the Python idbuild/idmkunix replacements over the generated manifest and pack.d tree.
    depends_on:
      - driver-packages-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_idbuild.py
          - --manifest
          - "{simple_idconfig_manifest}"
          - --cc
          - "{cc}"
          - --ld
          - "{ld}"
          - --output
          - "{cf_dir}/unix"
          - --cflag
          - -std=gnu89
          - --cflag
          - -fcommon
          - --cflag
          - -fno-builtin
          - --cflag
          - -O2
          - --cflag
          - -m32
          - --cflag
          - -D_KERNEL
          - --cflag
          - -DAT386
          - --cflag
          - -DWEITEK
          - --cflag
          - -DWEITEK_EMULATOR
          - --cflag
          - -DVPIX
          - --cflag
          - -DSYSV
          - --cflag
          - -DSVR40
          - --include-dir
          - "{cf_dir}"
          - --include-dir
          - "{workspace_root}/arch/at/uts/i386"
          - --include-dir
          - "{workspace_root}/uts/i386"

  idbuild-at386-floppy:
    description: Build the reduced AT386 unix image intended to fit on the boot floppy filesystem.
    depends_on:
      - driver-packages-at386-floppy
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_idbuild.py
          - --manifest
          - "{floppy_idconfig_manifest}"
          - --cc
          - "{cc}"
          - --ld
          - "{ld}"
          - --output
          - "{floppy_unix}"
          - --cflag
          - -std=gnu89
          - --cflag
          - -fcommon
          - --cflag
          - -fno-builtin
          - --cflag
          - -O2
          - --cflag
          - -m32
          - --cflag
          - -D_KERNEL
          - --cflag
          - -DAT386
          - --cflag
          - -DWEITEK
          - --cflag
          - -DWEITEK_EMULATOR
          - --cflag
          - -DVPIX
          - --cflag
          - -DSYSV
          - --cflag
          - -DSVR40
          - --include-dir
          - "{floppy_cf_dir}"
          - --include-dir
          - "{workspace_root}/arch/at/uts/i386"
          - --include-dir
          - "{workspace_root}/uts/i386"

  boot-floppy-kernel-at386:
    description: Strip the reduced AT386 unix image into the floppy replacement artifact.
    depends_on:
      - idbuild-at386-floppy
    steps:
      - kind: mkdir
        path: "{builddir}/boot-media"
      - kind: command
        argv:
          - sh
          - -eu
          - -c
          - |
              cp "{floppy_unix}" "{boot_kernel_stripped}"
              strip -s "{boot_kernel_stripped}"

  boot-at386:
    description: Build and stage the historical AT386 bootstrap family under uts/i386/boot into the synthetic system-root.
    depends_on:
      - prepare-layout
    steps:
      - kind: command
        argv:
          - sh
          - -eu
          - -c
          - |
              legacy_bin="{builddir}/uts/i386/tools/legacy-host/bin"
              mkdir -p "$legacy_bin" "{system_root}/etc"
              cat >"$legacy_bin/cc" <<'EOF'
              #!/bin/sh
              out=
              src=
              prev=
              for arg in "$@"; do
                if [ "$prev" = "-o" ]; then
                  out="$arg"
                fi
                prev="$arg"
                src="$arg"
              done
              if [ "${{src##*/}}" = "rmhdr.c" ] && [ -n "$out" ]; then
                cat >"$out" <<'EOS'
              #!/bin/sh
              set -eu
              objcopy -O binary "$1" "$2"
              EOS
                chmod 755 "$out"
                exit 0
              fi
              exec "{cc}" -m32 -std=gnu89 -fcommon -fno-builtin "$@"
              EOF
              chmod 755 "$legacy_bin/cc"
              cat >"$legacy_bin/as" <<'EOF'
              #!/bin/sh
              set -eu
              exec as --32 "$@"
              EOF
              chmod 755 "$legacy_bin/as"
              cp "{workspace_root}/tools/legacy_ld.py" "$legacy_bin/ld"
              chmod 755 "$legacy_bin/ld"
              stage_root="{legacy_stage_root}"
              mkdir -p "$stage_root/uts/i386"
              rm -rf "$stage_root/tools" "{boot_stage_root}"
              cp -a "{workspace_root}/tools" "$stage_root/"
              cp -a "{workspace_root}/uts/i386/boot" "$stage_root/uts/i386/"
      - kind: command
        argv:
          - python3
          - tools/uts_boot_symvals.py
          - --workspace-root
          - "{workspace_root}"
          - --boot-root
          - "{boot_stage_at386_root}"
          - --cc
          - "{cc}"
      - kind: command
        argv:
          - python3
          - tools/uts_build_boot.py
          - --workspace-root
          - "{workspace_root}"
          - --boot-root
          - "{boot_stage_root}"
          - --build-root
          - "{builddir}/uts/i386/boot/build"
          - --system-root
          - "{system_root}"
          - --cc
          - "{cc}"
          - --cpp
          - "{cpp}"
          - --as
          - as
          - --strip
          - strip
      - kind: command
        argv:
          - python3
          - tools/uts_boot_initprog.py
          - --workspace-root
          - "{workspace_root}"
          - --boot-root
          - "{boot_stage_root}"
          - --build-root
          - "{builddir}/uts/i386/boot/at386/initprog"
          - --system-root
          - "{system_root}"
          - --cc
          - "{cc}"
          - --cpp
          - "{cpp}"
          - --as
          - as
          - --ld
          - "{ld}"

  boot-floppy-hybrid-at386:
    description: Build a hybrid AT386 boot floppy image by combining the locally built fdboot with the official reference floppy filesystem and kernel payload.
    depends_on:
      - boot-at386
    steps:
      - kind: mkdir
        path: "{builddir}/boot-media"
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - build-hybrid
          - --reference-image
          - "{reference_boot_floppy}"
          - --bootloader
          - "{builddir}/uts/i386/boot/build/fdboot"
          - --output
          - "{hybrid_boot_floppy}"
          - --report-json
          - "{hybrid_boot_report}"

  boot-floppy-hybrid-at386-labeled:
    description: Build a hybrid AT386 boot floppy image and apply the repo-owned named replacement set from the boot floppy manifest.
    depends_on:
      - boot-at386
    steps:
      - kind: mkdir
        path: "{builddir}/boot-media"
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - build-hybrid
          - --reference-image
          - "{reference_boot_floppy}"
          - --bootloader
          - "{builddir}/uts/i386/boot/build/fdboot"
          - --output
          - "{hybrid_boot_labeled_floppy}"
          - --report-json
          - "{hybrid_boot_labeled_report}"
          - --replacement-manifest
          - "{hybrid_boot_replacement_manifest}"
          - --replacement-set
          - "{hybrid_boot_replacement_set}"

  boot-floppy-at386:
    description: Build an AT386 boot floppy image with the locally built fdboot and unix kernel while preserving the rest of the reference floppy filesystem as the rootfs.
    depends_on:
      - boot-at386
      - boot-floppy-kernel-at386
    steps:
      - kind: mkdir
        path: "{builddir}/boot-media"
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - build-hybrid
          - --reference-image
          - "{reference_boot_floppy}"
          - --bootloader
          - "{builddir}/uts/i386/boot/build/fdboot"
          - --output
          - "{boot_floppy}"
          - --report-json
          - "{boot_report}"
          - --replacement-manifest
          - "{hybrid_boot_replacement_manifest}"
          - --replacement-set
          - "{boot_replacement_set}"

  boot-floppy-at386-labeled:
    description: Build the AT386 boot floppy image with the locally built fdboot and unix kernel, then apply the repo-owned labeled replacement set.
    depends_on:
      - boot-at386
      - boot-floppy-kernel-at386
    steps:
      - kind: mkdir
        path: "{builddir}/boot-media"
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - build-hybrid
          - --reference-image
          - "{reference_boot_floppy}"
          - --bootloader
          - "{builddir}/uts/i386/boot/build/fdboot"
          - --output
          - "{boot_labeled_floppy}"
          - --report-json
          - "{boot_labeled_report}"
          - --replacement-manifest
          - "{hybrid_boot_replacement_manifest}"
          - --replacement-set
          - "{boot_labeled_replacement_set}"

  boot-floppy-validate-replacements-at386:
    description: Validate the local-kernel boot floppy replacement set against the reference image without writing a new floppy image.
    depends_on:
      - boot-floppy-kernel-at386
    steps:
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - validate-replacements
          - --image
          - "{reference_boot_floppy}"
          - --replacement-manifest
          - "{hybrid_boot_replacement_manifest}"
          - --replacement-set
          - "{boot_replacement_set}"

  boot-floppy-validate-replacements-at386-labeled:
    description: Validate the repo-owned named boot floppy replacement set against the reference image without writing a hybrid image.
    steps:
      - kind: command
        argv:
          - python3
          - tools/boot_floppy_image.py
          - validate-replacements
          - --image
          - "{reference_boot_floppy}"
          - --replacement-manifest
          - "{hybrid_boot_replacement_manifest}"
          - --replacement-set
          - "{hybrid_boot_replacement_set}"

  fp-at386:
    description: Build and stage the historical floating-point emulator family under uts/i386/fp into the synthetic system-root.
    depends_on:
      - prepare-layout
    steps:
      - kind: command
        argv:
          - sh
          - -eu
          - -c
          - |
              stage_root="{legacy_stage_root}"
              mkdir -p "$stage_root/uts/i386"
              rm -rf "{fp_stage_root}"
              cp -a "{workspace_root}/uts/i386/fp" "$stage_root/uts/i386/"
      - kind: command
        argv:
          - python3
          - tools/uts_fp_symvals.py
          - --workspace-root
          - "{workspace_root}"
          - --fp-root
          - "{fp_stage_root}"
          - --cc
          - "{cc}"
      - kind: command
        argv:
          - python3
          - tools/uts_build_fp.py
          - --workspace-root
          - "{workspace_root}"
          - --fp-root
          - "{fp_stage_root}"
          - --build-root
          - "{builddir}/uts/i386/fp/build"
          - --system-root
          - "{system_root}"
          - --cpp
          - "{cpp}"
          - --ld
          - "{ld}"

  historical-platform-at386:
    description: Aggregate the historically separate bootstrap and floating-point emulator build families.
    depends_on:
      - boot-at386
      - fp-at386

  os-at386:
    description: Compile the core operating-system sources under uts/i386/os.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - os-at386
        cwd: os
        output_dir: "{obj_root}/os"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  vm-at386:
    description: Compile the virtual-memory subsystem sources under uts/i386/vm.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - vm-at386
        cwd: vm
        output_dir: "{obj_root}/vm"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  disp-at386:
    description: Compile the scheduling and dispatcher classes under uts/i386/disp.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - disp-at386
        cwd: disp
        output_dir: "{obj_root}/disp"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  vx-at386:
    description: Compile the VP/ix support driver sources under uts/i386/vx.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - vx-at386
        cwd: vx
        output_dir: "{obj_root}/vx"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  exec-at386:
    description: Compile all executable-loader families under uts/i386/exec.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - exec-at386
        cwd: exec
        output_dir: "{obj_root}/exec"
        continue_on_error: false
        parallel: true
        discover:
          recursive: true
          extensions:
            - .c

  io-core-at386:
    description: Compile the generic and flat AT386 overlay driver sources under uts/i386/io, excluding ws, kd, and kdvm subtrees.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - io-at386
        cwd: io
        output_dir: "{obj_root}/io/core"
        continue_on_error: false
        parallel: true
        discover:
          recursive: true
          extensions:
            - .c
            - .s
          exclude_dirs:
            - ws
            - kd
            - kdvm

  io-ws-at386:
    description: Compile the workstation stream driver subtree under uts/i386/io/ws.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - io-at386
        cwd: io/ws
        output_dir: "{obj_root}/io/ws"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  io-kd-at386:
    description: Compile the AT386 keyboard and display driver subtree under arch/at/uts/i386/io/kd.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - io-at386
        cwd: io/kd
        output_dir: "{obj_root}/io/kd"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  io-kdvm-at386:
    description: Compile the AT386 KD virtual-memory companion driver under arch/at/uts/i386/io/kdvm.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - io-at386
        cwd: io/kdvm
        output_dir: "{obj_root}/io/kdvm"
        continue_on_error: false
        discover:
          extensions:
            - .c

  io-at386:
    description: Aggregate the generic, workstation, and AT386-specific driver subtrees.
    depends_on:
      - io-core-at386
      - io-ws-at386
      - io-kd-at386
      - io-kdvm-at386

  ml-at386:
    description: Build the low-level machine-layer objects, including the generated locore/start/syms kernel inputs.
    depends_on:
      - prepare-layout
    steps:
      - kind: command
        argv:
          - python3
          - tools/uts_ml_build.py
          - --workspace-root
          - "{workspace_root}"
          - --obj-root
          - "{obj_root}/ml"
          - --pack-root
          - "{pack_root}/kernel"
          - --cc
          - "{cc}"
          - --cpp
          - "{cpp}"
          - --ld
          - "{ld}"
          - --cflag=-std=gnu89
          - --cflag=-m32
          - --cflag=-fcommon
          - --cflag=-fno-builtin
          - --cflag=-O2
          - --cflag=-I..
          - --cflag=-D_KERNEL
          - --cflag=-DAT386
          - --cflag=-DVPIX
          - --cflag=-DWEITEK
          - --cflag=-DWEITEK_EMULATOR
          - --cpp-flag=-P
          - --cpp-flag=-I..
          - --cpp-flag=-D_KERNEL
          - --cpp-flag=-DLOCORE
          - --cpp-flag=-DAT386
          - --cpp-flag=-DVPIX
          - --cpp-flag=-DWEITEK
          - --cpp-flag=-DWEITEK_EMULATOR

  fs-at386:
    description: Compile the full filesystem stack, including nested filesystem types, under uts/i386/fs.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - fs-at386
        cwd: fs
        output_dir: "{obj_root}/fs"
        continue_on_error: false
        parallel: true
        discover:
          recursive: true
          extensions:
            - .c

  des-at386:
    description: Compile the kernel DES sources under uts/i386/des.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - netmod-at386
        cwd: des
        output_dir: "{obj_root}/des"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  rpc-at386:
    description: Compile the kernel RPC sources under uts/i386/rpc.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - netmod-at386
        cwd: rpc
        output_dir: "{obj_root}/rpc"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  ktli-at386:
    description: Compile the kernel TLI sources under uts/i386/ktli.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - netmod-at386
        cwd: ktli
        output_dir: "{obj_root}/ktli"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  klm-at386:
    description: Compile the kernel lock-manager sources under uts/i386/klm.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - netmod-at386
        cwd: klm
        output_dir: "{obj_root}/klm"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  netinet-at386:
    description: Compile the TCP/IP stack sources under uts/i386/netinet.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - netinet-at386
        cwd: netinet
        output_dir: "{obj_root}/netinet"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  network-at386:
    description: Aggregate the network-facing kernel modules that historical nonet builds excluded.
    depends_on:
      - des-at386
      - rpc-at386
      - ktli-at386
      - klm-at386
      - netinet-at386

  kdb-core-at386:
    description: Compile the in-kernel debugger core under uts/i386/kdb/kdb.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - kdb-at386
        cwd: kdb/kdb
        output_dir: "{obj_root}/kdb/core"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  kdb-gdebugger-at386:
    description: Compile the graphical debugger companion sources under uts/i386/kdb/gdebugger.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - kdb-at386
        cwd: kdb/gdebugger
        output_dir: "{obj_root}/kdb/gdebugger"
        continue_on_error: false
        discover:
          extensions:
            - .c

  kdb-util-at386:
    description: Compile the in-kernel debugger support sources under uts/i386/kdb/kdb-util.
    depends_on:
      - prepare-layout
    steps:
      - kind: compile
        source_view: uts-at386
        use:
          - kdb-at386
        cwd: kdb/kdb-util
        output_dir: "{obj_root}/kdb/kdb-util"
        continue_on_error: false
        parallel: true
        discover:
          extensions:
            - .c

  kdb-at386:
    description: Aggregate the kernel debugger families that participate in the AT386 kernel build tree.
    depends_on:
      - kdb-core-at386
      - kdb-gdebugger-at386
      - kdb-util-at386

  kernel-core-at386:
    description: Aggregate the non-network kernel subsystems that form the AT386 kernel proper.
    depends_on:
      - config-at386
      - os-at386
      - vm-at386
      - disp-at386
      - vx-at386
      - exec-at386
      - io-at386
      - ml-at386
      - fs-at386

  kernel-at386-nonet:
    description: Full AT386 kernel build graph without the network-facing subsystems.
    depends_on:
      - kernel-core-at386
      - kdb-at386

  kernel-at386:
    description: Full AT386 kernel build graph across the complete uts/i386 tree, including network and debugger subsystems.
    depends_on:
      - kernel-core-at386
      - network-at386
      - kdb-at386

  kernel-system-at386:
    description: Full AT386 kernel graph plus the historical platform families and staged system-build phases.
    depends_on:
      - kernel-at386
      - historical-platform-at386
      - master-tree-at386
      - idbuild-at386