diff --git xterm-clean/main.c xterm-workdir/main.c
index 9885e2c..d7988bd 100644
--- xterm-clean/main.c
+++ xterm-workdir/main.c
@@ -4551,7 +4551,7 @@ spawnXTerm(XtermWidget xw, unsigned line_speed)
 			/* make /dev/tty work */
 			ioctl(ttyfd, TCSETCTTY, 0);
 #endif
-#if ((defined(__GLIBC__) && defined(__FreeBSD_kernel__)) || defined(__GNU__)) && defined(TIOCSCTTY)
+#if ((defined(__GLIBC__) && defined(__FreeBSD_kernel__)) || defined(__GNU__) || defined(__mlibc__)) && defined(TIOCSCTTY)
 			/* make /dev/tty work */
 			ioctl(ttyfd, TIOCSCTTY, 0);
 #endif
diff --git xterm-clean/xterm_io.h xterm-workdir/xterm_io.h
index bf7014b..a862d21 100644
--- xterm-clean/xterm_io.h
+++ xterm-workdir/xterm_io.h
@@ -119,6 +119,10 @@
 #define USE_TERMIOS
 #endif
 
+#if defined(__svr4_reborn__)
+#define USE_POSIX_TERMIOS
+#endif
+
 #ifdef SYSV
 #define USE_SYSV_TERMIO
 #endif
@@ -170,10 +174,12 @@
  */
 #ifdef SYSV
 #ifdef USE_USG_PTYS
+#ifndef __svr4_reborn__
 #include <sys/stream.h>		/* get typedef used in ptem.h */
 #ifdef HAVE_SYS_PTEM_H
 #include <sys/ptem.h>		/* get struct winsize */
 #endif
+#endif /* !__svr4_reborn__ */
 #endif /* USE_USG_PTYS */
 #endif /* SYSV */
 
@@ -210,7 +216,7 @@
 #undef TIOCSLTC
 #endif
 
-#if defined (__sgi) || (defined(__linux__) && defined(__sparc__)) || defined(__UNIXWARE__)
+#if defined (__sgi) || (defined(__linux__) && defined(__sparc__)) || defined(__UNIXWARE__) || defined(__svr4_reborn__)
 #undef TIOCLSET			/* defined, but not usable */
 #endif
 
